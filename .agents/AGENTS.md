# Project Rules

- **No Hardcoded Secrets**: Do not hardcode any secrets (passwords, tokens, API keys, JWT secrets, database connection strings, etc.) directly in the codebase files. Always load them from environment variables via process.env or configuration files.
